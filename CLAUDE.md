# SafeClip — CLAUDE.md
_Last updated: 1 August 2026 (Session 19 — **SafeClip open-sourced (public GitHub repo, MIT), the marketing website fully redesigned and pushed all the way to production, and a local-only screen-recording toggle for marketing captures.** Reversed Session 8's closed-source pivot at the owner's initiative: ran a full git-history secrets audit (clean — every branch, every added/removed line across 47 commits; only hits were the security scanner's own test fixtures), updated `LICENSE`'s copyright line to the owner's real name, deleted a stray Session-8-era branch referencing an abandoned Polar.sh distribution direction, then flipped the repo public and verified with an **unauthenticated** request that it's genuinely visible, not just to the owner's own token. Built `App/DeveloperFlags.swift` + `CaptureProtection.swift` so the owner can record the app for previews: the panel/toast/color-picker loupe are excluded from screen capture by design (privacy feature) which also blocked *legitimate* marketing recording — first pass put a lever in the release binary itself, the owner correctly redirected to a **Debug-build-only local toggle that never ships**, git-`skip-worktree`'d automatically (new standing rule: local dev-flag files always get this, no asking). Finalized the app icon via a bespoke interactive drag/slider SVG editor (published as a Claude Artifact) — owner set exact final values (shield 0.9 down, both strokes 1.8) — which caught a real bug along the way (`qlmanage` baking white into the icon's transparent corners). **Website: full redesign shipped to beta, then explicitly promoted all the way to production** (`main`, a clean 25-commit fast-forward) — full-black theme, OCR/color-picker sections rebuilt as alternating two-column layouts with the owner's real screen recordings finally installed, `/thanks` redesigned around an actual rendered DMG-window image instead of an HTML mock, the hero rebuilt from a static mockup into a genuinely interactive demo (scrollable list, working tabs, live search, click-to-copy to the real clipboard) with a device-and-connection-adaptive wallpaper background — building that surfaced and fixed three real bugs in turn (a reveal-on-scroll animation that could leave the entire page at `opacity:0` if `IntersectionObserver` never fires; a wallpaper fade so weak the headline was briefly white-on-white illegible; then a "fix" with no flat plateau that kept dimming the whole lower two-thirds instead of holding original brightness — each verified afterward by literally sampling the composited pixels against WCAG contrast thresholds, not eyeballed) — plus copy tightened sitewide, all em dashes removed, pill badges replaced with bullet-separated text, and a real bug fixed where beta mode was charging the real $7 instead of being free. **Known gap for next session: version is bumped to 0.3.1/build 6 in `Config/Info.plist`, but no matching notarized DMG has actually shipped** — one was built mid-session and then went stale when the recording-toggle approach changed; a fresh `Scripts/release.sh` run is needed before 0.3.1 is real. Two next-version feature notes logged, not built: a menu-bar "time bomb" panic-wipe button, and Uninstall needing to clear TCC permission grants (Accessibility/Screen Recording), not just app data — sharpened with the owner's own hit-in-practice 5-step friction report.** Full detail in the Session 19 block below. Prior header, Session 18 — **app icon added + two onboarding first-run gaps fixed.** The app finally has an **AppIcon** (the website's shield-checkmark favicon rasterized to the 10 standard macOS sizes, wired into both targets via `ASSETCATALOG_COMPILER_APPICON_NAME` — placeholder until a hand-tuned 1024 exists). Onboarding fixes, owner-reported from a friend's install: (1) **capture is now gated on consent, not on onboarding-dismissal.** The friend opened the app *from the mounted DMG*, saw onboarding, was told to close-and-move it to Applications, reopened — and onboarding was gone, because the old gate wrote `onboardedVersion` the moment the window closed (a bare red-button/⌘W close counted the same as finishing), and that flag lives in `UserDefaults` (keyed on the bundle ID, **not** the path, so it survived the move). First fixed with an explicit `completed` flag (bare-close ≠ finish); then **superseded within the same session** by the cleaner model the owner asked for: **clipboard capture (`monitor.start()`) now runs iff `hasAcceptedTerms && hasAcceptedPrivacyPolicy`, and the wizard re-appears every launch until that consent exists** (plus once after a version bump for an already-consented user). This subsumes the bare-close fix (an unfinished first run has no consent ⇒ re-shows, no capture) **and** resolves the separately-flagged *capture-before-consent on Skip* gap — Skip/close record no consent, so capture stays off until the user actually accepts. The `completed`/`isFirstRun` plumbing was removed as redundant. (2) **Running from the DMG (or a Gatekeeper App Translocation path) is now caught at launch** — TCC grants + Sparkle updates pin to that randomized read-only path and are discarded on move, so any permission granted there is wasted; the app detects `/Volumes` or `/AppTranslocation` (never dev's DerivedData) and prompts "move to Applications first" before onboarding or capturing. Committed to `main` and **SHIPPED as 0.3.0 / build 5, live on the beta site** (notarized, stapled, Sparkle-signed; `release.sh` ran end-to-end from the automated shell — Finder-automation styled DMG and all — and the live beta appcast + DMG were verified byte-identical to the notarized artifact). `onboardingVersion` stays **2** (the wizard-reshow gate is separate from the app version). **One migration note:** anyone who Skipped under the *old* code (has `onboardedVersion` set, never consented) will now be re-prompted and capture pauses until they accept — the correct privacy-first outcome; normal Start-onboarded users are untouched.** Full detail in the Session 18 block below. Prior header, Session 17 — **website landing flow restructured (Hero → OCR → Color picker → Features → Privacy → Pricing → CTA → FAQ; "Three Steps" + Audiences sections deleted), OCR/color-picker turned into full-width screen-recording windows awaiting the owner's `.mp4`s, a new `/thanks` post-download install guide, a custom styled `.dmg` window in `release.sh`, and onboarding permissions consolidated behind a version-gated re-show.** Full detail in the Session 17 block below. Prior header, Session 16 — **0.2.2 SHIPPED AND LIVE on the beta site.** Two owner-reported bugs root-caused and fixed: the ⌥C "Capture canceled" pop-up was a **macOS 26 system HUD** (bare `screencapture -i` inherits the user's saved screenshot style — `window`/`video` here — and a rectangle drag cancels that session; fixed by forcing selection mode with `-s`), and the color-loupe "not working" in the web DMG was the **silent Screen Recording fallback** to `NSColorSampler`, not a stale build (now explains itself once, with an Open-System-Settings button). 0.2.2/build 4 notarized (Accepted, stapled, Sparkle-signed), `SUFeedURL` moved to the live beta Vercel URL, first real appcast `<item>` published; live DMG bytes, appcast fields, and the Ed25519 signature all verified end-to-end against the deployed site; installed /Applications copy upgraded from the update-dead-ended 0.2.1; stale 0.2.1 dmg removed from the site. Full detail in the Session 16 block below. Prior header, 3 July 2026, Session 15, cont. — **beta-prep round: "Send Feedback…" menu item + About-tab button (pre-filled mailto:, no server), an onboarding video player wired into 5 of the 6 wizard steps (falls back to the existing schematic mocks until real recordings are dropped in `App/OnboardingVideos/`), and a flagged decision that "SafeClip" needs a new name (collides with an existing app + video-editing tools — deferred to its own session).** Earlier same session: Sparkle auto-update wired in for the GitHub channel (menu-bar "Check for Updates…", a Settings → Updates tab, consent-gated automatic checks, Ed25519 signing key via CryptoKit, `PRIVACY.md`/README corrected). **Update (15 Jul): FIRST REAL NOTARIZED RELEASE SHIPPED — `SafeClip-0.2.1.dmg`, status Accepted, stapled, Sparkle-signed.** Apple Developer Program enrollment (approved 12 Jul) → Developer ID Application cert created → notarytool credentials stored — all owner-side, no code needed. Running `Scripts/release.sh` then surfaced **two real bugs in the script itself**, both fixed: (1) it used `xcodebuild build` instead of `archive`+`-exportArchive` — plain `build` always injects the `com.apple.security.get-task-allow` debug entitlement regardless of configuration/identity, which notarization rejects outright; (2) `Sparkle.framework`'s bundled pre-built helper binaries (`Autoupdate`, `Updater.app`, two XPC services) aren't touched by Xcode's automatic signing and need an explicit re-sign pass with the Developer ID identity before the outer app is sealed. First attempt came back `status: Invalid` with both errors from Apple's notarization log; second attempt (fixed script) came back `status: Accepted`. The `.dmg` is sitting in `dist/` locally — **not yet uploaded anywhere**, since `safeclip.app` isn't live yet (still Vercel beta URL). Still in progress: app icon, onboarding video recordings, finishing the website. TOP PRIORITY for next session: finish the website (pushing to Vercel for the friends beta first) and do the accumulated owner interactive QA, then start beta testing.**)._

> This file auto-loads at the start of every Claude Code session opened in this folder. It is the single source of truth for "what is this and what's next." Detailed product spec lives in [PRD.md](PRD.md).

---

## 🚦 SESSION HANDOFF — read first

**Status: BUILT + OCR + sync + auto-update + feedback channel + onboarding-video wiring shipped, the long-running panel scroll/click bug is ROOT-CAUSED AND FIXED (Session 14), and as of Session 19 the app repo is PUBLIC (MIT).** All milestone code (M0–M5) plus v0.2.0 (images/files), Session-5 OCR, the Session-7 warm-up bundle, Session-9 snippets, the Session-10 batch, Session-12's fixes, and Sessions 15–19's work below are implemented, build with **zero warnings** under Swift 6 strict concurrency (both `SafeClip` and `SafeClip-MAS`), **74 core tests pass**, and the automated security smoke test passes live (encrypted rows on disk, keychain key, dedup, relaunch persistence). Repo: [github.com/Mudit01100001/safeclip](https://github.com/Mudit01100001/safeclip) — **now genuinely public**, verified with an unauthenticated request (Session 19); a full git-history secrets audit came back clean first. Website ([github.com/Mudit01100001/safeclip-web](https://github.com/Mudit01100001/safeclip-web), private) had a full redesign shipped to **production** in Session 19 — see that block for what's live. **Known open items:** the app still needs a new name (see the Session-15 beta-prep block); **version is 0.3.1/build 6 in `Config/Info.plist` but no matching notarized DMG has shipped yet** — top priority for next session is a fresh `Scripts/release.sh` run; the website's real Lemon Squeezy checkout URL is still `REPLACE_ME` in production; **onboarding doesn't re-show on a reinstall and Uninstall doesn't clean up TCC permission grants** — confirmed hit-in-practice by the owner in Session 19, see "What remains" item 7 below, not built.

**Session 19 — open-sourced (MIT, public), website redesigned and promoted to production, local recording toggle (30 Jul–1 Aug):**

> A wide-ranging session: unblocking the owner's ability to record marketing previews, finalizing the app icon interactively, reversing the Session-8 closed-source decision, and a from-the-ground-up website redesign that went all the way to the live production domain.

- ✅ **Recording toggle for marketing previews — local-only, never in a shipped build.** The panel, the "copied" toast, and the color-picker loupe are excluded from screen capture (`sharingType = .none`) by design — a privacy feature that also stopped the owner from recording the app themselves. New `App/Services/CaptureProtection.swift` centralizes the decision; new `App/DeveloperFlags.swift` holds `allowScreenRecording` (committed `false`). **First pass put the lever on the release side** (a hidden `UserDefaults` flag, off by default) — the owner correctly redirected: *"the recording part is just a testing state"* — so `CaptureProtection` now returns the local flag in `#if DEBUG` and **hard-`false` in release, with no runtime lever at all**. `git update-index --skip-worktree App/DeveloperFlags.swift` applied immediately, and this is now a **standing rule**: any local dev-flag file gets skip-worktree'd automatically, never asked about (see memory `feedback_local_dev_flags_out_of_git`). Also dropped the stale `SAFECLIP_SCROLL_TEST` env var from the Debug scheme (Session-14 leftover; auto-popped the panel on every launch) so a Debug recording session starts clean.
- ✅ **App icon — final layout, set by the owner interactively.** Built a small standalone drag/slider SVG editor (published as a Claude Artifact — live preview, real-size strips at 128/64/32/16 on light/dark/installer backgrounds, draggable shield + tick, numeric readout) so the owner could position the mark exactly rather than iterate blind over chat. Final values: shield `translate(0, 0.9)`, both shield and tick strokes `1.8` (up from 1.5/1.6), tick centered. Caught a real bug in the process: `qlmanage`'s SVG rasterization was baking **white into the icon's transparent corners** — invisible on the white artifact preview background, glaringly obvious once composited onto the dark DMG background. Fixed by rasterizing through `NSImage` into a real transparent `NSBitmapImageRep` context instead (corner alpha verified `0.0`). Propagated everywhere: `AppIcon.appiconset` (all 10 sizes), the website favicon, `og.png`, and the DMG-window preview image — plus the website nav/footer brand glyphs and the `/thanks` file-icon mock, which had all been quietly using an **older, different** shield shape until this session.
- ✅ **DMG background finalized.** Owner supplied updated art (smaller arrow, no wordmark — they don't have the font); "SafeClip" composited back on in SF Pro semibold via the same Swift/CoreGraphics approach as the app icon. Installer window taken to a true 4:3 (`Scripts/release.sh` bounds `{200,120,840,628}`, icon centers `{160,240}`/`{480,240}`; `Scripts/dmg_background.swift` geometry kept in lockstep and now carries a warning that running it overwrites the owner's hand-made art with the schematic fallback).
- ⚠️ **Version bumped to 0.3.1 (build 6) — NOT actually released.** A 0.3.1 DMG *was* built and notarized mid-session (to validate the recording fix on a real stable install, since the Xcode Debug build kept re-signing and losing its Screen Recording TCC grant — three duplicate `SafeClip.app` copies sharing one bundle ID turned out to be the real cause of the reported "permission asks in a loop"). That artifact went stale the moment the recording-toggle approach changed from a release-side flag to the Debug-only local file, so **it was never pushed anywhere** and no notarized 0.3.1 exists today despite the version number already being bumped in `Config/Info.plist`. **Top priority next session: a fresh `Scripts/release.sh` run**, which will also carry the finalized icon and DMG background live for the first time.
- ✅ **SafeClip (the app) is open source again — MIT, public repo.** Owner-initiated reconsideration ("I think it's pretty well decided... that's what improvement is about") reversing Session 8's closed-source pivot; `safeclip-web` explicitly stays private. This was actually *un*-reversing an even earlier call — Session 5's original plan was exactly "MIT source public for audit, paid notarized `.dmg` for convenience," which Session 8 abandoned. Before flipping visibility: **(1) full git-history secrets audit** — every filename ever added across all branches (103, none suspicious), the full added+removed diff content across all 47 commits on both `main` and a stray branch (23,805 lines) checked against known secret signatures (private-key headers, AWS/GitHub/Stripe/Slack token formats, Apple app-specific-password format), the Sparkle/notarization scripts (confirmed Keychain-only), `SUPublicEDKey` (confirmed intentionally-public key material), the sync feature's secret handling (randomly generated + Keychain-stored, no hardcoded default), and entitlements (empty/minimal as documented) — **clean**; the only pattern-matches were AWS's own official `EXAMPLE` key and synthetic tokens inside `SecurityScannerTests.swift`, testing the app's own detection feature. **(2) License** — turned out to already exist: `LICENSE` has been a valid, correctly-formed MIT file since the repo's very first commit, a leftover from the original plan that Session 8 never removed; only the copyright line needed fixing, from the generic "SafeClip Contributors" to the owner's real name, **Mudit Ahlawat** (`23a787d`). Also deleted a stray `claude/github-download-portability-3luatw` branch (dated 15 Jun, referencing an abandoned Polar.sh distribution direction — nothing secret in it, just stale). Flipped to public via `gh repo edit --visibility public`, then **verified with a fully unauthenticated `curl`** (200, LICENSE readable, no token) — genuinely public, not just visible to the owner's own session. One quiet consequence: `TERMS.md`, `PRIVACY.md`, and `README.md` had all claimed "open source, MIT, github.com/.../issues" the entire time the repo was actually private (Session 8 through today) — that standing false claim is now finally true, not something this session had to go fix.
- ✅ **`safeclip-web`'s GitHub default branch switched `main` → `beta`** (unrelated to the above, resolved via the same conversation about the owner's contribution graph — private-contributions-on-profile was off, now on). Metadata-only: confirmed both the production (`main`-deployed) and beta Vercel URLs kept serving 200 unaffected, since Vercel's Production Branch setting doesn't follow GitHub's default branch after initial import.
- ✅ **Website: full redesign, shipped to beta then explicitly promoted all the way to production.** Owner's instruction was consistently "beta site only" through most of the session (one earlier attempt to fast-forward `main` was correctly blocked by the permission classifier) until an explicit **"push it to production"** at the end, at which point `main` was fast-forwarded to `beta` — a clean 25-commit, zero-conflict fast-forward, verified live on `safeclip-web.vercel.app` afterward. What shipped:
  - **Visual system**: true black background + flat neutral (de-blued) grey surfaces, replacing the diagonal grey gradients that read as generic/AI-templated. Nav bar height 64px→56px; brand mark everywhere (nav, footer, favicon, OG image, DMG preview) is now the bare shield glyph, no tinted square behind it, all matching the finalized app icon.
  - **OCR + color-picker sections** rebuilt as alternating two-column layouts (copy left/video right, then mirrored) instead of stacked — a real space win on a desktop-sized window, which is what this Mac app is actually for. The owner's real screen recordings arrived and got installed: both were exported square (1:1) by the recording tool, handled per-clip rather than force-uniforming — the color-picker capture was a *static* letterbox (content box identical at every timestamp, confirmed by sampling), so its dead bars were safely cropped to ~16:10; the OCR capture used the recording tool's auto zoom/pan (content box measurably drifts across the clip), so a fixed crop risked clipping the "Text copied" toast and it was kept square. Both re-encoded for the web (14MB → 1.3MB combined). Copy for both sections tightened to 2-3 short sentences each (was 4 sentences of marketing-brick prose).
  - **`/thanks`** (post-download install guide) restructured to a Kiruna-style hierarchy (small "STEP" label → big bold Open/Install/Launch → one sentence → preview), and its Install-step mockup swapped from a crude hand-built HTML/CSS DMG-window imitation to an **actual rendered image** (`dmg-window.png` — the real background art + the real app icon + the real Applications-folder icon, composited via the same Swift/CoreGraphics pipeline used for the DMG background itself) inside simple HTML window chrome (just the traffic lights).
  - **Hero section rebuilt from a static mockup into a genuinely interactive demo** — the floating-panel recreation now actually scrolls (10 clips in a fixed-height container), has working Snippets/All tabs, live search-as-you-type filtering with an empty state, and **clicking a row copies it to the visitor's real clipboard** with the app's own selected+"Copied" confirmation animation. Row trailing details ("3m", "58 chars · 12m", snippet triggers) and the bottom hotkey hint bar were switched from low-contrast grey to white after the owner flagged them as illegible against the new wallpaper-backed panel.
  - **Hero wallpaper** — the owner supplied a bright, high-contrast chrome/metal abstract image. Getting this right took several real iterations, each one owner-corrected toward a better answer: *(1)* first attempt dimmed/blurred the image via a CSS filter to force contrast — owner explicitly redirected: *"don't dim the image... the brightness and contrast is what makes it alive"* — replaced with a **pure black-fade overlay** (same technique as the existing bottom-of-section fade, just extended). *(2)* A later "taller bottom fade" request was implemented with no flat middle zone, so the gradient interpolated continuously from 55% down to 98% opacity — the *entire lower two-thirds kept darkening* instead of holding at original brightness, which read as "the image is too dim" even though the top/bottom treatment itself was working; fixed by adding a genuine flat `transparent` plateau (50%–80% of section height, later widened to 74%) so the panel's actual vertical position (measured at 61.7% of the section) sits in guaranteed-original-brightness territory. *(3)* Text contrast was never eyeballed — verified each time by literally drawing the image + the exact CSS gradient onto an offscreen canvas, sampling the real pixels behind the headline/subhead, and computing WCAG relative-luminance contrast ratios (final: headline 15.4:1, subhead 6.6:1 worst-case single pixel, both clearing the 4.5:1 AA minimum with real margin). Finally made **device- and connection-adaptive**: three tiers of the same image (720w/1280w/1920w — 1920 being the source file's own native ceiling, not a chosen cap) with an inline script picking one based on the wallpaper element's rendered size × `devicePixelRatio`, then only ever downgrading via the Network Information API (`saveData`/`effectiveType`; Safari has no such API and silently falls back to device-only selection).
  - **Copy pass sitewide**: every em dash removed, the pill-shaped trust-chip badges replaced with plain bullet-separated text (`Local-only • No account • On-device OCR • Zero permissions to start`), and a real bug fixed where **beta mode was showing the real `$7` price** instead of being free (beta is meant to be a friends-test direct download, no payment).
  - **Reveal-on-scroll safety net.** While debugging the wallpaper contrast, found that the site's `.js-reveal` mechanism hides *every* `.reveal` element until `IntersectionObserver` reports it — so anything that stops that callback from ever firing (confirmed happening in this session's own headless browser pane: 0 of 27 elements revealed) leaves the **entire page at `opacity: 0`**. Added a 1.2s fallback that force-reveals anything still hidden, so the worst case is a missed fade-in animation, never a blank page.
  - **Production checkout is still `REPLACE_ME`** — pre-existing, not introduced this session, but now live on a much more polished/compelling site, so more visitors are likely to actually click "buy" and hit a dead Lemon Squeezy URL. Flagged to the owner before the production push; not fixed (no real checkout URL exists yet).
- ✅ **Two next-version feature notes logged, not built** (per established practice: log clearly, defer, don't build speculatively): a menu-bar **"time bomb" panic-wipe button** (bomb icon → "Delete all data" in red → wipes history/data, with an erasing-text animation, also planned for app-open); and **Uninstall needs to clear TCC permission grants** (Accessibility, Screen Recording), not just app data — this was already logged from Session 18's reinstall-onboarding request, and got sharpened this session with the owner's own **hit-in-practice** report: they redownloaded from the DMG, onboarding didn't reappear (confirming the logged bug is real, not theoretical), and described the exact 5-step manual dance (remove from Accessibility by hand → reopen → re-ask → toggle on → reopen again) needed to work around stale TCC grants after a reinstall. Recommended fix direction noted: an install-fingerprint gate (bundle creation date) for the onboarding re-show, and `tccutil reset` shelled out from the Uninstall flow for the permissions cleanup — neither implemented.

**Session 18 — app icon + onboarding first-run gaps fixed, capture now consent-gated (25 Jul):**

> Owner watched a friend install the beta and hit a real first-run trap: the friend opened the app straight from the mounted DMG, onboarding showed, the owner told them to close it and move the app to Applications, and on reopen onboarding never came back — leaving a half-configured install that never accepted terms. Root-caused to two gaps (below), then the owner asked to also close the related capture-before-consent hole, which produced the cleaner consent-gate that subsumes gap #1. Also finally gave the app an icon.

- ✅ **App icon (`7afdc62`).** The app had no `AppIcon.appiconset` at all. Rasterized the website's shield-checkmark brand mark (`safeclip-web/public/favicon.svg`) — via `qlmanage -t` since no SVG rasterizer (rsvg/inkscape/magick) is installed, then `sips` down to the ten standard macOS sizes (16→512 @1x/@2x) — into `App/Assets.xcassets/AppIcon.appiconset` with a matching `Contents.json`, and wired `ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon` into **both** targets in `project.yml`. Reads cleanly down to 16px (checked the rendered strip). Verified `AppIcon.icns` is embedded + referenced in the built `Info.plist`. **Explicitly a placeholder** — the still-open "make a 1024×1024 hand-tuned icon" item stands; this just stops the generic-app-icon look for the beta.
- ✅ **Capture is now gated on consent, not on onboarding-dismissal (`e8867d8` first pass → `a2157dc` final).** *The bug:* three paths wrote the `onboardedVersion`/`hasCompletedOnboarding` gate — *Start*, *Skip*, **and `windowWillClose`** (red button / ⌘W) — so a bare close counted as finishing; because the gate lives in `UserDefaults` (domain = bundle ID, path-independent) it survived the DMG→/Applications move and onboarding never returned. *First pass* threaded an explicit `completed: Bool` (bare-close ≠ finish) with an `isFirstRun`-aware persist. *Superseded same session* (owner asked to also stop Skip from capturing without consent) by the **final, cleaner model:** the launch gate is now `if hasConsent { monitor.start() }` where `hasConsent = hasAcceptedTerms && hasAcceptedPrivacyPolicy`, and `if !hasConsent || effectiveVersion < onboardingVersion { presentOnboarding() }`. So **capture never runs without consent**, and the wizard **re-appears every launch until consent exists** (plus once after a version bump for an already-consented user). `presentOnboarding()`'s completion OR-merges consent and only calls `monitor.start()` when `terms && privacy`. This single mechanism subsumes the bare-close fix *and* the capture-before-consent gap, so the `completed`/`isFirstRun` plumbing was removed (`OnboardingWindowController` back to a one-arg completion). **Migration:** anyone who Skipped under the old code (has `onboardedVersion`, never consented) is now re-prompted and capture pauses until they accept — the correct privacy-first outcome; normal Start-onboarded users (both consent flags true) are untouched and keep capturing with no re-show.
- ✅ **Running from the DMG / translocated path is caught at launch (`e8867d8`).** The friend's deeper problem: running from the *mounted disk image*. Apps run from a `.dmg` or a Gatekeeper **App Translocation** path have their TCC permission grants and Sparkle updates pinned to a randomized, read-only path that macOS **discards when the app is moved** — so any Screen Recording / Accessibility grant made there is wasted. `AppDelegate` now gates launch on `isRunningFromDiskImageOrTranslocated()` (`Bundle.main.bundlePath` starts with `/Volumes/` or contains `/AppTranslocation/` — dev builds run from DerivedData under `$HOME`, so this **never fires while developing**) and shows a "Move SafeClip to your Applications folder" `NSAlert` (**Quit** default terminates; **Continue Anyway** is an escape hatch for the rare external-volume install), skipping onboarding + capture on that run. Together with the consent-gate this closes the friend's whole round-trip: open-from-DMG nudges to move first; a first run never captures or persists anything until consent.
- ⏳ **Owner QA (same accessory-app automation limit as Sessions 12–17 — can't drive the wizard synthetically):** on a fresh install (delete the `com.mudit.safeclip` defaults domain first), (1) open onboarding, click **Skip** (or close with the red button) → nothing should be captured, and reopening the app should re-show the wizard; (2) click through to **Start Using SafeClip** with both boxes checked → capture begins and the wizard doesn't return; (3) mount the beta DMG and open the app *from the DMG* → the move-to-Applications alert should appear and Quit should terminate; (4) confirm the new icon shows in Finder / the Dock switcher / About.
- ✅ **SHIPPED as 0.3.0 / build 5, LIVE on the beta site (25 Jul).** Version bumped `0.2.2/4 → 0.3.0/5` (`c380b4c`; minor bump chosen over 0.2.3 to mark the first-run behaviour change). `Scripts/release.sh` ran end-to-end (network + `safeclip-notary` creds + the styled-DMG Finder automation all worked from the automated shell this time): notarization **Accepted**, stapled, Sparkle-signed. `SafeClip-0.3.0.dmg` — sha256 `ba10b709…68be2e1`, `length="5418095"`, `edSignature="OXlQ7nWX…AeWyxBg=="`. Website `beta` branch (`ea33f16`): dmg at `public/downloads/SafeClip-beta-0.3.0.dmg`, `betaCta.href` bumped, new appcast `<item>` (sparkle:version 5), superseded 0.2.2 dmg removed. **Verified against the live beta deploy:** appcast serves 0.3.0/build 5 as newest, the DMG returns 200 at 5 418 095 bytes, and the deployed bytes are byte-identical to the notarized artifact (sha256 match). `SUFeedURL` is still the beta Vercel URL (migrate both feed + enclosures to `safeclip.app` when the domain is live). `onboardingVersion` stays **2** (the wizard-reshow gate is separate from the app version). The styled 4:3 DMG (owner's hand-made background + composited wordmark) shipped in this build — the Session-17 styled-DMG code ran for real for the first time.

**Session 17 — website landing-flow restructure, /thanks install guide, styled DMG, onboarding permissions consolidation (18 Jul):**

> Owner review: the landing page read as AI-generated and buried the two visual features; separately, permissions were surfacing as scattered first-use pop-ups because onboarding never re-appeared.

- ✅ **Website landing flow restructured** (`safeclip-web` beta `d196a9e`, `cc2c383`). New order: **Hero → OCR → Color picker → Features → Privacy → Pricing → CTA → FAQ → Footer**. **Deleted `HowItWorks.astro` ("Three Steps") and `Audiences.astro`** (+ their `site.ts` exports and the dead `.step-line` CSS) — don't try to re-import them. Privacy section retitled **"What You Copy Is Nobody's Business"**. Footer lost the giant wordmark watermark; "Made with ♥ by Mudit" → **"Made by Mudit"**. Removed the "Signed and notarized by Apple…" line (`betaCta.instructions`) and its three uses.
- ✅ **OCR + color picker are now full-width screen-recording windows.** `ShowcaseFrame.astro` gained a `video` prop (muted/loop/playsinline; a reduced-motion script in `Layout.astro` pauses `video[data-rm-pause]`). Each section does a **build-time `existsSync`** on `spec.filename` and renders `<video>` if present, else a "Screen recording" placeholder. **Owner drops in `public/assets/recording-ocr.mp4` / `recording-picker.mp4`** (H.264, ~8–12s seamless muted loop, 16:10). Verified both states with a throwaway mp4. **A running `astro dev` needs a restart to notice a new file; the prod/Vercel build always picks it up.** The old CSS teardrop loupe mockup was **removed** — it was sized for the half-width column and looked lost at full width.
- ✅ **New `/thanks` post-download page** (Kiruna-style Open → Install → Launch, CSS mockups incl. a DMG-window mock, `noindex`, auto-starts the `.dmg` + manual fallback link). Beta CTAs route through it via the new **`primaryCtaHref`** export (`showPayment ? links.checkout : "/thanks"`), so **it only appears in beta mode** — local `npm run dev` and production go to checkout instead, which is why it can look "missing."
- ✅ **Custom styled `.dmg` window** (app `288a850`). New `Scripts/dmg_background.swift` renders `Scripts/assets/dmg-background@2x.png` (dark canvas, wordmark, squiggly arrow → Applications); `release.sh` packaging reworked to **UDRW → Finder icon layout via `osascript` → UDZO convert**. Tested end-to-end on a throwaway image (`.DS_Store` layout persists, valid checksum). **Geometry constants in the Swift renderer and the AppleScript window bounds must stay in lockstep.** First real run needs a one-time "Terminal wants to control Finder" consent; the shipped 0.2.2 dmg still has the stock window until the next `release.sh` run.
- ✅ **Onboarding permissions consolidated + version-gated re-show** (`08d0450`). The wizard's step 4 now offers, per permission, **Enable** *and* **Open Settings** deep-links (`?Privacy_Accessibility` / `?Privacy_ScreenCapture`) with find-it steps, and live status refreshed on `didBecomeActive` (green check without a relaunch). **Root cause of "setup never re-appears":** the gate was the `hasCompletedOnboarding` bool, which survives app deletion (same bundle ID ⇒ same defaults domain). Replaced with `AppDelegate.onboardingVersion` (**2**) vs a stored `onboardedVersion`; legacy flag counts as v1. **Bump that constant only on material onboarding changes, not per release.** First run still waits for consent before capturing; a re-show keeps capturing, pre-checks prior consent via `OnboardingView.init(initialConsent:)`, and the completion **ORs** consent so closing a re-show can't erase an earlier acceptance. Owner confirmed the only bar is "a new user's first download works" — it does (no defaults ⇒ effectiveVersion 0 ⇒ wizard shows).
- ⏳ **Owner QA still needed:** click through onboarding step 4 on a fresh build (accessory app can't be automated — same limitation as Sessions 12–16). For repeat testing on the *same* build use Settings → About → "Show Onboarding Again"; the version gate won't re-fire.

**Session 16 — 0.2.2: OCR "Capture Canceled" HUD + silent loupe fallback fixed; release live on the beta site (18 Jul):**

> Two bugs the owner hit while testing the beta download, both of which looked like SafeClip bugs and were actually interactions with macOS/permissions — worth reading before touching `screencapture` or the loupe again.

- ✅ **⌥C OCR "Capture canceled" pop-up on every capture — root-caused, fixed with one flag.** The string exists **nowhere in SafeClip's source, git history, or compiled binary** — it's macOS 26's own screenshot HUD. Cause: `ScreenOCRService.runScreencapture` ran `screencapture -i` *without forcing a capture mode*, so `-i` inherited the user's saved `com.apple.screencapture` `style` pref (`window`, with `video = 1` on the owner's machine). The interactive session therefore started in window/video mode; dragging a rectangle *cancels* that session (→ system "Capture Canceled" HUD, every time) while still saving the rectangle (→ OCR kept working, masking the bug). Reproduced live before fixing: the un-flagged command even **captured the wrong region** (a window under the cursor, not the drag). **Fix: `-s` added to the args** (selection-only mode — the exact UX this feature wants). Validated by CLI reproduction of the same invocation. **Rule for the future: never shell out to `screencapture -i` without pinning the mode (`-s`/`-J selection`).**
- ✅ **Loupe "not working as intended" in the web DMG — it was never a stale build.** Loupe code in every shipped DMG is byte-identical to HEAD; a fresh install just lacks Screen Recording, so `ScreenColorPicker.pick()` silently fell back to the plain `NSColorSampler` (owner confirmed: "plain round magnifier"). The Xcode build only looked healthier because dev runs had already been granted the permission. **Fix:** `explainLoupeFallbackOnce()` — a one-time `NSAlert` (UserDefaults key `screenRecordingLoupeExplained`) explaining the permission + an "Open System Settings" button (`…?Privacy_ScreenCapture`), shown before falling back. Never nags again.
- ✅ **0.2.2 (build 4) released end-to-end and LIVE.** `Scripts/release.sh` → notarization **Accepted**, stapled, Sparkle-signed (`SafeClip-0.2.2.dmg`, sha256 `c5c57181…`, 5 126 418 bytes). `SUFeedURL` moved from the unregistered `safeclip.app` to the **stable beta Vercel URL** (`safeclip-web-git-beta-mudit-s-projects3.vercel.app/appcast.xml`) — owner-approved since nobody had downloaded the beta yet; migrate to `safeclip.app` via a normal Sparkle update once the domain is live. Website `beta` branch: DMG at `public/downloads/SafeClip-beta-0.2.2.dmg`, `betaCta.href` updated, **first real appcast `<item>`** (edSignature + length from the release script). **Verified against the deployed site:** live DMG is byte-identical to the notarized build; live appcast serves the right version/build/length; the appcast's Ed25519 signature validates over the live DMG bytes using the installed app's `SUPublicEDKey`; installed build (4) == appcast build (4), so "Check for Updates…" reports up-to-date. The full Sparkle release round-trip flagged "never tested" in Session 15 is now proven, minus only the human click on the menu item.
- ✅ **Version-audit cleanup:** /Applications had 0.2.1 (build 3) whose baked-in feed URL is the dead `safeclip.app` — **0.2.1 installs can never self-update**; replaced manually with the notarized 0.2.2 (TCC grants persist across the upgrade — same bundle ID + Developer ID). Superseded 0.2.1 dmg removed from the website (`a1370c4`). Production (`main`) still carries the empty appcast scaffold — deliberate, nothing points at it until `safeclip.app` exists.
- ℹ️ **Automation note (same limitation as Sessions 12–15):** the app's global shortcuts (⌥C/⌥P) and menu-bar UI still can't be driven by computer-use/synthetic events, and the computer-use server can't even resolve the accessory app by name — the OCR fix was validated by reproducing the exact `screencapture` invocation at the CLI, and Sparkle by cryptographic verification instead of clicking. **Owner QA (30 s):** ⌥C → drag → text copies with **no bottom pop-up**; ⌥P on a fresh-permission machine → the new explanation alert appears once; menu bar → "Check for Updates…" → Sparkle's "You're up to date."
- 🔧 **In parallel (owner's own session, same day):** website landing-flow redesign shipped to beta (`d196a9e` — `/thanks` install-guide page that auto-starts the download, full-width recording showcases) and `release.sh` gained a styled-DMG step (`288a850` — Finder icon layout + background via `Scripts/dmg_background.swift`; first styled release will need the one-time "Terminal wants to control Finder" Automation consent).

**Session 15 (cont.) — beta-prep round: Send Feedback, onboarding video player, rename flagged (3 Jul):**

> With the owner about to send builds to friends, this round covers the code-side gaps that don't need Mudit's own action: a way for testers to actually report bugs (no telemetry by design means silence otherwise), and wiring so the onboarding wizard can show real screen-recordings once Mudit records them, instead of the schematic mocks.

- ✅ **"Send Feedback…"** — new `App/Services/Feedback.swift` opens a pre-filled `mailto:m14ahlawat@gmail.com` (subject has version+build, body has the macOS version) via `NSWorkspace.shared.open`. Zero network code, matches the existing no-telemetry posture. Wired into the menu bar (`MenuBarController`, next to "Check for Updates…") and the About tab (`AboutView`, next to "Show Onboarding Again"). Verified the generated `mailto:` URL percent-encodes the em dashes/unicode correctly in isolation before wiring it in.
- ✅ **Onboarding video player** — new `App/Onboarding/OnboardingVideoPlayer.swift` (`OnboardingVideo` view: `AVQueuePlayer` + `AVPlayerLooper` wrapped in an `NSViewRepresentable`, muted, looping, no native player chrome — `AVKit.VideoPlayer` was the wrong tool here since it shows controls on hover). Now backs 5 of `OnboardingView`'s 6 illustration steps (Terms, step 5, keeps its static seal icon), falling back to the pre-existing schematic mocks (`panelMock`, `pasteMock`, etc.) whenever the step's named `.mp4` isn't in the app bundle — which is the case today, so the fallback path is what's actually shipping until recordings exist. `project.yml` gained an `App/OnboardingVideos` resources entry (both targets, `optional: true`) and a README in that folder with the exact filenames Mudit needs to use plus the full recording spec/shot list (mirrored in memory `project_safeclip_onboarding_revamp`). **Verified:** both targets rebuild zero-warning and 74 tests still pass with the folder empty (i.e. the no-videos-yet state is confirmed non-breaking).
- ✅ **Flagged, not built: SafeClip needs a new name.** Owner noted "SafeClip" collides with an existing app and several video-editing tools/services. Logged to memory `project_safeclip_rename` rather than acted on — deliberately deferred to its own session. The memory records why this isn't a trivial rename: the bundle ID `com.mudit.safeclip` is load-bearing for Keychain item names (including the master encryption key and the Session-15 Sparkle signing key), the `UserDefaults` domain, and the on-disk history path — changing it needs an explicit migration, or a decision to keep the bundle ID forever and only change the display name/marketing.
- ℹ️ **Owner is separately, in parallel:** ~~starting~~ **Apple Developer Program enrollment is now APPROVED (12 Jul)** — next owner steps are creating the "Developer ID Application" cert and running `xcrun notarytool store-credentials` (see `Scripts/release.sh` header for exact commands); no code was blocked on this since the release script already takes the cert identity as an env var. Still open: making a 1024×1024 app icon (repo currently has none), and recording the 5 onboarding videos per the shot list above. The website is being actively worked on and will be pushed to Vercel for the friends-only beta (not the real `safeclip.app` domain yet) — when that URL exists, `SUFeedURL` in `Config/Info.plist` and the appcast enclosure URLs need to move there.

**Session 15 — Sparkle auto-update, GitHub channel only (3 Jul):**

> Beta testers need a way to get new builds without a manual re-download every time; Apple only handles this automatically for App Store apps, so a non-MAS `.dmg` needs [Sparkle](https://sparkle-project.org), the standard open-source framework for this.

- ✅ **`UpdateService.swift`** (`App/Services/`) wraps `SPUStandardUpdaterController`, gated behind an `OTHER_SWIFT_FLAGS: -DSPARKLE_ENABLED` compiler flag set **only** on the `SafeClip` target in `project.yml` (the `Sparkle` SPM package is likewise only a dependency of that target, never `SafeClip-MAS` — confirmed the MAS build strips `Sparkle.framework` as a stale artifact and builds zero-warning without it; Apple's MAS guidelines forbid self-updating apps anyway). `UpdateService.isSupported` is `false` on MAS, matching the existing `CaretLocator.isSupported` pattern so UI code doesn't need `#if` sprinkled everywhere.
- ✅ **Owner's explicit call on defaults:** the app makes **zero automatic network calls** unless the user opts in. `SUEnableAutomaticChecks` starts `false` in `Config/Info.plist`; a **"Check for Updates…"** menu-bar item (next to Settings/About) always works for a manual, one-off check; a new **Settings → Updates** tab (`UpdatesSettingsView.swift`) adds an "Automatically check for updates" toggle that — like the caret-anchoring Accessibility opt-in — shows a consent `NSAlert` explaining exactly what the check does *before* turning it on. `SUEnableSystemProfiling` is off (no OS/hardware/version telemetry on checks), and `SUAutomaticallyUpdate` is off (always shows release notes + asks before installing; never silent).
- ✅ **Signing key generated with zero manual steps** — Sparkle's EdDSA scheme is standard Ed25519 (RFC 8032), so instead of installing Sparkle's own `generate_keys`/`sign_update` CLI (would've needed Homebrew), a small script used Apple's own CryptoKit (`Curve25519.Signing`, already used elsewhere in the app) to generate the pair and write the private half straight into the login Keychain (service `com.mudit.safeclip.sparkle`, account `ed25519-private-key`) — it was never printed or logged. The public half is baked into `Info.plist` as `SUPublicEDKey`. **New `Scripts/sparkle_sign.swift`** reads that Keychain entry and signs a `.dmg` for the appcast; `Scripts/release.sh` now calls it automatically after notarizing and prints the ready-to-paste `<enclosure>` attributes. **Owner note:** this Keychain-write happened only after an explicit "Go for it" — the auto-mode classifier correctly blocked the first attempt because an earlier reply ("what is Sparkle, explain before building") hadn't actually greenlit key generation yet; worth remembering that a clarifying question isn't the same as approval.
- ✅ **Website wiring** — `safeclip-web/public/appcast.xml` scaffolded (valid empty Sparkle RSS feed + a commented template for the next `<item>`) so `SUFeedURL` (`https://safeclip.app/appcast.xml`) doesn't 404 today; `docs/DISTRIBUTION.md` documents the 3-step release flow (run `release.sh` → upload the `.dmg` to `safeclip.app/downloads/` → add the printed attributes as an `<item>`). No real release has gone through this pipeline end-to-end yet — first real use will be the next actual version bump.
- ✅ **Honesty pass on existing claims:** `PRIVACY.md` §1/§3/§4 and `README.md`'s "no telemetry, no analytics" line previously said the app "has no network functionality whatsoever" — now corrected to disclose the opt-in/manual update check specifically (no clipboard content, no device profile, signed-and-verified downloads only). This is the only network-claim change; nothing else about local-only storage or zero-content-transmission changed.
- ⏳ **Owner QA needed (couldn't verify interactively this session — same known limitation as prior sessions: this is an `.accessory`/`LSUIElement` app with no dock icon, and computer-use/AppleScript automation can't reliably drive it; see Session 12's identical note).** Verified instead via: clean zero-warning build of both targets, all 74 core tests passing, package resolution pulling Sparkle 2.9.3 cleanly, unified log showing no crash/error on a fresh launch, and the MAS build's build log explicitly showing `Sparkle.framework` being stripped as stale. **What's still unconfirmed by a human:** the "Check for Updates…" menu item actually appears and is clickable; the Settings → Updates tab renders correctly and the consent alert appears before the automatic-check toggle flips on; clicking "Check for Updates…" against the current (essentially empty) `appcast.xml` shows Sparkle's native "you're up to date" dialog without crashing; a full real release round-trip (bump version → `release.sh` → appcast `<item>` → app finds and offers to install it) has never been tested.

**Session 14 (cont.) — resolved a real git divergence with `origin/main` and pushed (2 Jul):**

> Sessions 7–14 existed only in the local repo (origin was still at Session 4). Separately, a different Claude session (via claude.ai/code, 14 Jun) had pushed its own commits directly to `origin/main` from that same Session-4 point — a DPDP Act 2023 consent flow, `PRIVACY.md`, and an unsigned-GitHub-release CI workflow — without either side ever syncing. Neither side was disposable: local had the confirmed-working scroll/click fix plus Sessions 7–13's shipped features; origin had real legal-compliance work.

- ✅ **Merged, not discarded either side.** `OnboardingResult` (separate Terms/Privacy/Marketing consent, each unchecked by default per DPDP §6) now drives the whole onboarding flow; the 6-step wizard's final step shows proper separate Terms + Privacy Policy checkboxes (both required) and an optional marketing checkbox, using origin's `consentRow` component. New `LegalDocumentSheet.swift` shows `TERMS.md`/`PRIVACY.md` in an in-app sheet instead of opening the file externally. `PRIVACY.md` and TERMS.md's Grievance Officer (§11) / Privacy Policy reference (§10) sections came in from origin. `AppDelegate` now persists `hasAcceptedPrivacyPolicy`/`hasAcceptedMarketing`/`privacyPolicyVersion` alongside the existing `hasAcceptedTerms`.
- ✅ **Dropped `.github/workflows/release.yml`** (owner decision, confirmed "DMG-first") — it automated free unsigned `.zip` GitHub Releases, which directly conflicts with the Session-8 decision to sell a paid, notarized `.dmg` from the website only. README/TERMS §7 keep the paid/notarized framing; the unsigned-build language from origin was not merged in.
- ✅ **One real landmine avoided:** git's 3-way merge silently auto-resolved `OnboardingWindowController.swift`'s completion-closure type to origin's `OnboardingResult` in a region with **no conflict markers** (correct per 3-way-merge rules — HEAD hadn't touched that exact line since Session 4, so only origin's change applied) — but `AppDelegate.swift`'s existing `presentOnboarding()` helper, sitting just below the marked conflict, still called it with the old `Bool` signature and would have silently failed to compile if only the marked conflicts had been "resolved." Full manual reconciliation of `OnboardingView.swift` (local's 6-step wizard as the base, origin's consent UI folded into step 6) plus updating that unconflicted helper was required — **a lesson for any future merge here: check unconflicted regions near a marked conflict, don't trust that "no markers" means "no work needed."**
- ✅ Version bumped to 0.2.1 (from origin, auto-merged cleanly; ahead of local's stale 0.2.0). Both schemes build zero-warning, 74 core tests pass. Pushed — `origin/main` now matches local (`566fd3f`).
- ✅ **New standing rule (owner instruction):** push to `origin` at every session close, not just commit locally — `git fetch` + check for divergence first, don't blind-push. This is what caught the above.

**Session 14 — scroll/click ROOT-CAUSED AND FIXED, permission-free (2 Jul):**

> The Session-13 fork in the road (CGEventTap vs. living with the bug) is CLOSED — the misrouting was self-inflicted, not an OS limitation on `.accessory` apps.

- ✅ **Root cause (proved, not inferred):** the window server derives a non-opaque window's **mouse-event region from its rendered pixel alpha** (below ~0.05 = click-through), and the panel was a borderless `backgroundColor = .clear` window whose Liquid Glass surface contributes almost nothing to the window's own backing store — to pointer routing the panel barely existed. Proof: a new SELFTEST diagnostic logs `event.windowNumber` on globally-observed events inside our frame and resolves the owner via `CGWindowListCopyWindowInfo` — **all 37 misrouted events in the n=20 baseline (17/20 scrolls + 20/20 clicks) were delivered to the window directly behind the panel**, at every post-show delay from 0.1s to 1.5s (killing any fade-in/stale-render theory). Activation state was always irrelevant — the window server routes pointer input by pixels, not by key/active status, which is why Sessions 9–13's activation work never moved the needle.
- ✅ **The fix is one shape fill** (`ClipboardPanelView`): the callout is backed by `shape.fill(Color.black.opacity(0.06))` behind the glass — just above the hit-test threshold, shape-accurate (hit region and window shadow still match the visible callout; the transparent beak-strip corners still pass clicks through, as they should). **SELFTEST after: scroll 20/20, click 20/20, `misrouted_dst{}` empty — zero misrouted events; clicks land natively (no replay, no forwarding).** Confirmed on three consecutive n=20 runs (incl. a window-`backgroundColor`-alpha variant of the same idea that also scored 20/20 before being replaced by the shape-accurate fill).
- ✅ **Broken Session-13 click-replay REMOVED.** `panel.sendEvent(event)` on a global-monitor event was doubly broken: `locationInWindow` on a global event is in *screen* coordinates (so the replayed click hit the wrong point in panel space), and the matching mouse-up was never forwarded (so a tap could never complete — hence baseline `click_replayed_but_missed=20/20`). A click inside our frame seen by the global monitor is now only *counted* (`click_misrouted`) as a regression signal — never replayed, never treated as "click outside."
- ✅ **Global monitors kept as inert regression insurance** (deliberate deviation from "delete them"): they no longer fire for panel-bound events (zero cost, zero leak), but macOS 26.3 RC and the 26.4 beta both shipped Apple-acknowledged regressions in exactly this transparent-window hit-testing (FB21879057/FB21879511) — if it breaks again, scroll degrades to "forwarded with a possible echo" instead of silently dead, and `misrouted_dst` names the destination immediately.
- ✅ **Stale docs corrected:** the panel HAS activated SafeClip on `show()` since Session 12 round 3 — this file's later "non-activating again" claims described an intermediate state, not the shipped code; `FloatingPanelController` comments now match reality (activation is for keys/search; mouse routing is the pixel-alpha story above).
- 🧰 **Harness upgrade:** the SELFTEST report line now includes `panel_win=<n> misrouted_dst{Owner#win:count}` and `click_misrouted=<n>`. Re-run: `launchctl setenv SAFECLIP_SELFTEST 20` → launch the Debug build → read `/tmp/safeclip-selftest.txt` → `launchctl unsetenv SAFECLIP_SELFTEST`. Hide Xcode first (the Session-13 caveat still applies if the OS ever regresses).
- ✅ **Security audit (same session) + 3 hardening fixes.** Full static + live audit passed: no clipboard content readable in the on-disk DB (`strings`-verified against the real 132-row store), dedup hash is keyed HMAC (no confirmation attacks), zero network code, entitlements clean, sync E2E, OCR temp file deleted on all paths. Findings + accepted risks live in memory `project_safeclip_security_audit`. Fixed the three concrete gaps: (1) **⌃⌘1…0 quick-paste now respects `historyHidden`** — it was bypassing Privacy Mode / screen-recording hiding entirely (placed the item + showed a content toast; the panel's own paste paths always guarded this). (2) **Hidden-panel rows are now masked at the source** (`privacyHidden` on `ClipRowView`/`SnippetRowView` → dots, no hover reveal, no tooltip, plus `.accessibilityHidden(true)`) — the 10pt blur alone left the real strings in the view/accessibility tree, and outer SwiftUI redaction can't reach rows inside the native scroll view's own `NSHostingView`. (3) **`SnippetExpander` hard-stops while `IsSecureEventInputEnabled()`** — macOS already withholds keys from global monitors during secure entry, but that's observed behavior, not contract; now enforced in-code and the trigger buffer is dropped. **Accepted risks (owner's list, deliberately unfixed):** plaintext metadata columns (source app, sizes, timestamps — content stays encrypted), unsigned self-write pasteboard marker (only a native app could spoof it; web/ClickFix payloads can't set custom UTIs), sync LWW wall-clock merge + shared-secret tombstone trust (v1 design), default login-keychain ACL (documented upgrade path), and concealed/password-manager content being captured-encrypted by default (explicit owner decision, `captureConcealed=true`).
- ⏳ **Owner QA (quick):** (1) ⌥V → scroll immediately with the trackpad ×10 — the app behind must never scroll; (2) click rows, including off-center, ×10 — must paste, panel must never close without pasting; (3) eyeball the callout — the 6% black backing should be invisible against the glass (if it ever reads as a tint, step toward 0.05 and re-run SELFTEST); (4) toggle Privacy Mode → panel rows must show dots under the blur, and ⌃⌘1 must show the "Hidden while screen recording" toast instead of pasting.

**Session 13 — automated self-test harness root-caused the scroll/click bug to real OS-level input misrouting; found the fix's ceiling (2 Jul):** *(superseded by Session 14 — the "OS-level misrouting" was real but had a permission-free cause; kept for the methodology lessons)*

> **Read this before touching scroll or click again — it's a list of exactly what NOT to retry, because each was already tried this session and failed or was reverted.**

- ✅ **Built a real automated test harness (KEEP, worth extending) — `FloatingPanelController.runSelfTest(iterations:)`, gated behind `SAFECLIP_SELFTEST=<N>` (set via `launchctl setenv SAFECLIP_SELFTEST 10`, never baked into the checked-in scheme because it moves the real cursor and posts real clicks).** Posts genuine `CGEvent`s through `.cghidEventTap` — critically, *not* direct method calls — so a pass actually means real input works, not just that the Swift method can be called. Replaced hours of "rebuild → click/scroll N times by hand → paste back logs" with one rebuild + one aggregate line in `/tmp/safeclip-selftest.txt`. **Caveat learned the hard way (see below): don't leave Xcode's own clickable UI frontmost while it runs.**
- ✅ **Diagnostic-methodology lesson — do not repeat:** the pre-existing `SCROLLDIAG`/`m1`/`m2` self-check (Session 12) called `scrollView.contentView.scroll(to:)` and `scrollView.scrollWheel(with:)` **directly, in-process**. That only proves the scroll view *can* be told to move — it completely bypasses real event delivery, so it reported success (`m2=1`) every single time even when real trackpad input failed for the owner. **Any future scroll/click diagnostic must exercise the real delivery path (a real posted `CGEvent`, not a direct method call) or it will lie.**
- ✅ **Root cause found with certainty (not a guess):** instrumenting `forwardScroll` showed scroll failures were **0% `NSScrollView`'s fault** (`fwd_no_move=0` — once an event reaches us, it always scrolls) and **0% "lost" events** (`missed_seen_nowhere=0`) — **100% of failures were real events the window server routed to some *other* destination instead of SafeClip's local `NSEvent` monitor**, even while `NSApp.isActive`/`panel.isKeyWindow` read `true`. This is genuine OS-level input misrouting to an `.accessory` (`LSUIElement`) app's window, not a bug in our SwiftUI/AppKit code.
- ✅ **Click hitbox bug #1 — FIXED, confirmed, keep as-is:** `NSHostingView.acceptsFirstMouse(for:)` defaults to `false`, so a click on a not-yet-key window only focused it instead of registering the tap (classic AppKit "works, but only the 2nd click" trap). Fixed with `ClickThroughHostingView` (`NativeScrollView.swift`) overriding `acceptsFirstMouse → true`, applied to both the panel's own `hostingView` (`FloatingPanelController`) and the list's row-hosting view. **This part is solid — do not revisit.**
- 🟡 **Scroll fix shipped, but with a known, currently-unfixable side effect — `globalScrollMonitor` in `installKeyMonitor()`.** A global `NSEvent` monitor for `.scrollWheel` catches events the local monitor misses and manually calls `forwardScroll(event)`. **Verified via SELFTEST: scroll went 3/10 → 10/10, twice in a row.** But global monitors can only *observe*, never *consume/block* — **this is an OS API restriction, not a gap in our code** — so whatever the window server actually routed the event to (e.g. Claude Desktop, sitting behind the panel) *also* processes it. Owner confirmed live: the window behind still scrolls **~25% of the time**. **Do not look for a way to "just consume it anyway" from a global monitor — there isn't one.**
- 🟡 **Click fix attempted, NOT yet confirmed — `outsideClickMonitor` in `installKeyMonitor()`.** SELFTEST proved (`click_self_closed=10/10`) the panel-closing bug wasn't a hitbox-size problem: our own "click outside closes the panel" feature (added this session) was catching the *same* misrouted clicks the scroll bug produces and closing the panel before the row's tap could fire — a global monitor can't tell "click on the Finder desktop" apart from "our own click that got misrouted." Fix: check `panel.frame.contains(...)` first; if the click is inside our own frame, replay it via `panel.sendEvent(event)` instead of hiding. **This is still in the code but the owner's live re-test after this fix still saw the panel close on off-target clicks — treat it as unconfirmed/likely still insufficient, not "done."**
- ❌ **Tried and REVERTED — temporary `.regular` activation policy in `show()`/`hide()`.** Theory: `.accessory` apps might not be a reliable input-routing target for the window server even when "active," so temporarily calling `NSApp.setActivationPolicy(.regular)` while the panel is open (reverting on hide) might fix delivery. **Did not fix the leak** (owner's live test) **and introduced new instability** — while testing it, Xcode itself rebuilt/relaunched SafeClip repeatedly with nobody clicking Run (looked exactly like a crash-loop; several different PIDs a few minutes apart in `ps aux`). Root cause of *that*: while `.regular` was active, SELFTEST's synthetic `CGEvent` clicks — being real system-level events — were themselves misrouted to whatever was genuinely frontmost (Xcode, during dev testing), accidentally clicking Xcode's own Run button. **Reverted both changes entirely. Do not retry activation-policy juggling** without treating this instability as a real risk, not a testing artifact — and if SELFTEST is ever re-run, don't leave Xcode's own UI frontmost/clickable during the click-test portion, since misrouted synthetic clicks aren't guaranteed to stay confined to SafeClip's own window.
- ✅ **Paste-confirmation UX — FIXED, confirmed good by owner, no further changes needed.** Click-to-paste delay `0.1s → 0.45s` (`PanelViewModel.clickPaste`/`clickPasteSnippet`); highlight strengthened — accent-color background opacity `0.45 → 0.6`, added a 2pt accent-color border stroke, added a "✓ Copied" capsule badge (`ClipRowView` + `SnippetRowView` in `ClipboardPanelView.swift`).
- ✅ **Git-archaeology dead end — confirmed, don't re-ask "can we get an old working version back."** Every commit through `HEAD` (`5cd5cdc`, Session 8) predates the entire scroll-fix saga: `LazyVStack` has been in every committed version since the very first scaffold (`89d0320`), and the Session-8 code even carries a comment recording that a still-earlier NSView-injection attempt (pre-Session-8) was tried and reverted for "breaking scroll" further. **There is no historical commit — local or on GitHub — with a working scroll implementation to recover.** All of Sessions 9–13's scroll work exists only in the current uncommitted working tree. GitHub (`origin/main`) is even further behind (`b2f3a47`, Session 4) — Sessions 7 and 8 were committed locally but never pushed.
- ⏳ **Open decision for the owner — the actual fork in the road, needs a decision before more code changes here:** `NSEvent` local/global monitors have a hard ceiling — local doesn't reliably fire for misrouted events, global can observe but never consume. **The only mechanism that can both catch AND swallow a misrouted event system-wide is a `CGEventTap` created in `.defaultTap` mode** (not `.listenOnly`), which requires Accessibility/Input Monitoring permission. That permission is currently only requested for *opt-in* features (caret-anchoring, snippet auto-expand) — making core panel scroll/click depend on it means either prompting for Accessibility by default (against the "launches with zero special permissions" pillar) or shipping scroll/click semi-broken for anyone who declines. **Do not implement a `CGEventTap` for this without the owner explicitly deciding on the permission trade-off first.**

**Session 12 — fixed the 3 open Session-11 bugs + 2 new features (26 Jun):**

> **Round 5 (PROVED scroll works; the confound was a stale debugger-held instance).** Couldn't drive the panel via computer-use (accessory app isn't resolvable; osascript keystrokes blocked by Accessibility), so added a **file-based self-diagnostic**: `SAFECLIP_SCROLL_TEST=1` env makes the app auto-open the panel on launch and write `/tmp/safeclip-scroll-diag.txt`. Result on the freshly-built binary: `docH=5684 clipH=345 scrollable=true rows=184 panelKey=true appActive=true | scrollTest moved=true` — i.e. content is far taller than the viewport AND the scroll mechanism moves it. **Scroll is correct in the current source.** Root cause of "still broken": the owner was testing a **stale instance launched by Xcode's debugger** (pid persisted across `kill -9` because debugserver owns it; `open` reactivated it instead of loading the rebuilt binary) — the classic stale-build trap this file warns about. **Fix on owner side: in Xcode press Stop (■) then Run (▶), or quit SafeClip from the menu bar before relaunching.** Debug-only scaffolding left in: `sharingType = .readOnly` in DEBUG (panel is screenshot-able while debugging; release stays `.none`), and an `SAFECLIP_SCROLL_TEST`-gated auto-open+diagnostic. Reverted the temporary `.regular` activation policy (back to `.accessory`). Folder sync labelled **Beta**.
>
> **Round 4 (owner: scroll STILL broken after activating fix).** Found the *actual* root cause via web research (Apple/Swift forums): the list used a **`LazyVStack` inside the native scroll view's `NSHostingView` document**, which only lays out visible rows and so never reports the full content height — the document was never taller than the viewport, so there was nothing to scroll, regardless of activation. Fix: `LazyVStack` → `VStack` in both `listView`/`snippetListView` (history is capped ~200, so fine) + `.frame(maxWidth:.infinity, maxHeight:.infinity)` on each `NativeScrollView` so the scroll view fills the panel. The activation change (round 3) still stands — it's what stops the wheel leaking to the app behind. Folder sync relabelled **Beta**. Still owner-QA-only.
>
> **Round 3 (after owner QA: scroll worked but leaked to the app behind it on Cursor; OCR showed no structure; loupe still 2 shapes earlier):** (a) **Scroll leak** — root cause: a *non-activating* panel doesn't capture the wheel, so the active app underneath scrolled too (and the global forwarder couldn't consume). Fix: panel now **activates SafeClip on show** (`NSApp.activate`, dropped `.nonactivatingPanel`) so it captures scroll exclusively; removed the global scroll monitor (kept the local consume+forward). Focus still handed back on dismiss. (b) **Structure-aware OCR** — fixed a too-sensitive paragraph-break threshold (`height * 0.8` → `* 1.2`) that inserted blank lines between table rows; verified table reconstruction (cells → aligned rows) vs naive newline-join. Note: single-column prose looks identical to before (structure only changes tables/columns/reading-order). (c) Confirmed loupe is a single silhouette path now.
>
> **Round 2 (same day, after owner QA found scroll/focus/loupe still wrong):** (a) **Focus** — re-added explicit `restoreFocus(to: previousApp)` on the paste/Escape/toggle paths (kept OFF for click-outside). Non-activating alone wasn't keeping the user's app active in practice; the explicit re-activation guarantees ⌘V lands in their field without re-clicking. (b) **Scroll** — added belt-and-braces on top of the `NSScrollView`: `hosting.sizingOptions = [.intrinsicContentSize]` (so the document is actually taller than the viewport) **plus** local+global `scrollWheel` monitors in `FloatingPanelController` that forward wheel events over the panel straight to the list's `NSScrollView` via `scrollWheel(with:)` (works regardless of key/active state; local consumes to avoid double-scroll). (c) **Loupe** — `TeardropLoupeShape` rebuilt as ONE continuous silhouette path (major arc + rounded cone) instead of ellipse+triangle, so there's no overlap region that the fill turned into a visible cutout; glass simplified to tinted interactive + a clean rim stroke. (d) **Onboarding** — page-0 illustration is now a realistic mini SafeClip panel (search field, varied rows incl. a masked password + highlighted selection, hint bar) instead of gray bars. **Still owner-QA-only (panel is capture-excluded).**

- ✅ **Panel scroll + focus regression FIXED (the right way this time).** Root-caused both to the Session-9 "make the panel activating" approach. (A) **Scroll** — the list is now a real `NSScrollView` (`App/Panel/NativeScrollView.swift`: `NSScrollView` + flipped `FlippedView` document + `NSHostingView` of the SwiftUI rows). `NSScrollView` handles the wheel natively regardless of key/activation state, so SwiftUI's `ScrollView` (which never delivered wheel events here across Sessions 8–11) is gone from both `listView`/`snippetListView`. Arrow-key scroll-to-selection is preserved via `reportsSelectedRowFrame(_:into:)` (GeometryReader → `Binding<CGRect>` → `scrollToVisible`, not a `@Sendable` preference). (B) **Focus** — `FloatingPanel` is **non-activating again** (`.nonactivatingPanel` + `canBecomeKey` override only; `canBecomeMain` removed) and `show()` no longer calls `NSApp.activate`, so opening the panel never steals activation from the user's app. `hide()` lost its `restoreFocus:` param; focus is only handed back (via `previousApp.activate()`) **after a ClickFix confirmation modal**, which is the only path that activates SafeClip. **Still never synthesizes ⌘V.**
- ✅ **Loupe hex position** — swapped so hex sits at the **bottom** in the normal state and moves to the **top** when flipped near the screen bottom (`ColorLoupeView`, the `.position(y:)` for `hexPill`).
- ✅ **Loupe polish** — brighter/HDR-leaning glass: `glassEffect(.regular.tint(.white.opacity(0.22)).interactive(), …)` + a top-lit radial highlight filled into the shape (a fill, not a stroke, so no bulb/cone seam) + `.brightness(0.06)`. Tip rounded via `addArc(tangent1End:tangent2End:radius:)` in `TeardropLoupeShape` (~3% of side).
- ✅ **Onboarding dot spacing** — `pageDots` now has `.padding(.top, 22)` so it sits clear of the illustration.
- ✅ **#10 structure-aware OCR** — `SafeClipCore/.../OCR/TextLayout.swift` reconstructs reading order, column spacing, and paragraph breaks from Vision observations (pure + unit-tested); `OCRService.recognizeText` feeds blocks through it. Used by both ⌥C screen capture and image-text indexing.
- ✅ **#12 E2E folder sync (experimental, OFF by default).** Owner chose **folder-based (any cloud)** transport. Core (tested): `Sync/SyncKeyMaterial` (HKDF subkeys from a shared 32-byte secret + recovery phrase ↔ hex), `SyncRecord`/`SyncPayload`, `SyncMerge` (LWW by a shared `syncHash`, tombstone-wins ties, order-independent), `SyncCodec` (AES-GCM whole-file), and `HistoryStore.exportLiveRecords`/`applySync` (insert/update-metadata/delete-by-tombstone; cross-device identity is the shared `syncHash`, NOT the device-keyed `content_hash`). App: `App/Services/SyncService.swift` (writes `<deviceID>.scsync`, reads peers, merges, applies; debounced + 30s timer + on-activation + manual; tombstones persisted in UserDefaults; secret in Keychain account `sync-secret`) + **Settings → Sync** (`SyncSettingsView`: choose folder, recovery-phrase show/copy/import, Sync Now, status). `AppState` gained `onWillDelete`/`onLocalChange` hooks + `copyToClipboard` (phrase copy is marked-as-ours so it's never captured). **v1 semantics: additive union + LWW metadata + explicit-delete tombstones; synced items aren't auto-pruned (documented in the UI).**
- ⏳ **Interactive QA pending (owner):** the panel is `sharingType = .none` (excluded from screen capture), so scroll/focus can't be verified by screenshots — needs the owner's eyes. Check: (1) ⌥V → wheel + trackpad scroll the list; (2) after picking an item the previously-active window stays active and ⌘V lands there (incl. the ClickFix "Paste Anyway" path); (3) ⌥P loupe hex/glass/tip; (4) Sync across two Macs pointed at the same iCloud/Dropbox folder using the recovery phrase.

**Session 11 — QA round on Session-10 batch (26 Jun); 1 fix shipped, 3 bugs open (all resolved in Session 12 above):**
- ✅ **Settings scrollbars** — repeating 0.4s timer (`SettingsWindowController.startScrollerStyling`) now catches all tabs as they lazily load; owner confirmed fixed.
- 🔴 **Panel scroll + focus regression** — two separate problems. (A) Scroll is fully broken (mouse and trackpad). Root cause: `canBecomeMain = true` makes SwiftUI ScrollView eligible, but the current panel/app activation chain is still fighting it — needs rebuild as native **NSScrollView/NSTableView** (stop patching). (B) **Focus-restoration regression**: panel is actively kicking user out of the previously-active window when it hides. The `windowDidResignKey → hide(restoreFocus: false)` path and `previousApp.activate()` are interfering. **Fix in next session: (1) rebuild panel list in AppKit NSTableView for reliable scroll; (2) audit the hide/restoreFocus logic so hiding the panel does NOT steal activation from the app the user was in.**
- 🔴 **Loupe hex position inverted** — hex should appear at the **bottom** of the loupe in normal state and move to the **top** when near the screen bottom (currently reversed). Fix: in `ColorLoupeView`, the `hexPill` y-position logic inside `.position(x:y:)` needs to flip: `y: flipped ? hexBand / 2 : (hexBand + side + hexBand / 2)`.
- 🔴 **Loupe visual polish** — two issues: (1) Not "liquid glass enough" — the `glassEffect(.regular, …)` on the teardrop needs to be `.interactive` or the whole view brightness ramped; consider wrapping with a `.colorMultiply(.white.opacity(1.4))` tint or using `glassEffect(.prominent, …)` for the HDR pop the owner wants. (2) Tip too sharp — `TeardropLoupeShape` currently draws the cone to a mathematical point; add a small arc at the tip (`addArc(center: tip-offset, radius: ~4pt, …)`) to round it.
- ✅ **Onboarding page dots spacing** — owner noted dots sit too close to the illustration. Fix: increase `.padding(.bottom, 14)` on `pageDots` (or add `.padding(.top, …)` above the dots).

**Session 10 — feature batch (24 Jun), shipped:** (1) **Scroll fix attempt** — `canBecomeMain = true` on `FloatingPanel` + removed all scroll monitors. Did not fix scroll (see Session 11 above). (2) **Eyedropper v2 — rebuilt for SAFETY after it froze the Mac** (a hang: overlay grabbed input with no teardown). Now bulletproof: watchdog (30s), resign-active teardown, global+local Esc/click, cursor stays visible, idempotent `finish()`. Visuals (v2 icon → `Assets.xcassets/ColorPickerLoupe`): tip = pick point, vertical flip near screen bottom, **color ring** of sampled color + zoomed pixels, Liquid Glass; 140pt. Falls back to `NSColorSampler` until Screen Recording granted. Loupe hex position and polish still open (Session 11 bugs above). (3) **Onboarding revamp** — dark glass card, illustration, page dots, icon+title, inline controls, prominent Continue, back chevron; 6 steps (welcome→⌥Return→shortcuts→privacy→permissions→Terms). **TERMS.md bundled** + opened locally. Dot spacing tweak still open. (4) **#11 auto-expand snippets** — opt-in + Accessibility-gated; encrypted `trigger` (schema **v6**), `SnippetExpander` (global keyDown buffer → synth backspaces + `keyboardSetUnicodeString`, own events tagged). **⚠️ Needs owner QA with Accessibility granted** — synthesizes keystrokes into the focused app.

**Session 9 — panel-scroll fix + saved snippets (24 Jun):** (1) **🟢 Panel-scroll bug FIXED** — dropped `.nonactivatingPanel` so the floating panel now *activates* SafeClip on show and the SwiftUI list receives `scrollWheel` through the normal responder chain (a background non-activating panel never gets them). `FloatingPanelController` records the previously-frontmost app on show and **restores its focus on hide** (Escape/paste/toggle restore; click-outside doesn't, since the user's click already moved focus) so the user's own ⌘V still lands in their document. Removed the failed live-hit-test scroll monitor. **Still pillar-safe — never synthesizes ⌘V.** ⚠️ *Needs owner interactive QA: confirm scroll works AND ⌘V lands in the right app after the panel closes (esp. the ClickFix "Paste Anyway" modal path).* (2) **🟢 Saved snippets (#9) shipped** — encrypted `snippets` table (schema **v5**: label+body AES-GCM + `sort_order`), `HistoryStore` CRUD/reorder/`fetchSnippets`, `AppState.pasteSnippet` (places body via `PasteService.placePlainText` for ⌘V — not re-captured), new **Settings → Snippets** tab (`SnippetsSettingsView`: add/edit/reorder/delete), and a **"Snippets" chip** in the panel chip-bar that switches the list to snippet mode (search + arrows + Return paste; ⌘⌫/⌘P no-op there; blurred while screen-recording like history). +6 core tests. (3) **🟢 Magnifier eyedropper BUILT + WIRED** (owner supplied the icon → `App/Assets.xcassets/ColorPickerLoupe`). New `App/Services/ColorLoupe.swift`: `LoupeSampler` (an **actor** caching `SCShareableContent`, capturing a 25px patch/frame, throttled, excluding our own windows so it never magnifies itself) + `ColorLoupeView` (live magnified pixels `.interpolation(.none)` clipped to the icon's transparent hole — geometry **(0.5, 0.5609) r 0.2323** measured from the icon alpha — teardrop frame on top, hex caption below). `ScreenColorPicker` rewritten: lockout-safe `KeyableOverlayWindow` + cursor-following loupe window, click→copy hex, Esc cancels; `AppDelegate.pickColor` calls it. **Graceful fallback to `NSColorSampler`** until Screen Recording is granted (`CGPreflightScreenCaptureAccess` triggers the prompt) and in the sandboxed build. ⚠️ *Full-screen overlay + live capture can't be unit-tested — needs owner QA on a real display.* (4) **🔵 Scroll DEBUG tracker** — passive, non-consuming `scrollWheel` logger (`FloatingPanelController.trackScroll`, DEBUG only) prints `SafeClip scroll: …` so we can confirm whether the activating panel receives scroll events. **Onboarding revamp:** owner is sending a mockup to match (then build the 5-step wizard; **link TERMS.md only, no separate Privacy Policy**). Full carry-over in memory `project_safeclip_next_session.md`.

**Session 7 — competitive reckoning (24 Jun):** competitor **Supaste** leveled up (OCR, notch shelf, categories, inline snippets) and research surfaced **Maus** as the real threat — it owns "opens at your cursor / multi-monitor," so **caret-anchoring is no longer a unique wedge**. The only *uncontested* differentiators are all **safety** (encryption, plain-text default, screen-share blur, ClickFix warnings) — and clipboard threats just went mainstream (ClickFix +500% YoY, Apple Terminal paste-warnings in macOS 26.4, macOS 16 clipboard-privacy prompt). **Decision: the product/website lead with SAFETY for the general user; caret-anchoring is supporting.** A full 12-feature backlog ranked by implementation complexity lives in **private notes** (`~/.claude/plans/…supaste…md`) — deliberately **not** committed here (publish the code, not the playbook). Website scaffolding is a separate session.

**Session 8 — QA-fix rounds + closed-source pivot (24 Jun):** repo set **PRIVATE**; product is now **closed-source + paid** ("first in a series of QoL apps"). Built across three QA rounds: backlog #5–#8 (categories, multipaste, menu-bar history, image-OCR search), then fixes/polish — ClickFix-vs-concealed priority fix, dynamic file icons (real `NSWorkspace` doc/folder icons + extension-visible names), numeric quick-paste modifier picker, collapsible settings descriptions (`ExpandableText`), blur-not-blank, caret-proximity anchoring (slider + live preview, default 500pt), screenshot protection (`sharingType=.none` on panel+toast), "Appeared in images" search section, **em dashes removed from all copy**, and in-app open-source scrub (AboutView/Info.plist; README/LICENSE deferred to website session). 55 tests, both targets zero-warning. **Two known bugs were opened here; status after Session 9:** (1) **panel scroll — ✅ FIXED in Session 9** (panel made activating + prior-app focus restored on hide; pending owner interactive QA). (2) **custom magnifier eyedropper — 🟡 PARTIAL:** the window-lockout root cause is fixed (`KeyableOverlayWindow`), but `ScreenColorPicker.swift` is **still disabled/unwired** (⌥P keeps the safe system `NSColorSampler`) until the magnifier + owner's custom Illustrator icon (teardrop frame + transparent centre circle = the magnified window) land. Full carry-over in memory `project_safeclip_next_session.md`.

**Default shortcuts (remapped Session 5):** **⌥V** opens the floating panel; **⌥C** captures a screen region → OCR → text on the clipboard. Both rebindable in Settings → General. A one-time migration (`shortcutsV2Migrated` default) resets the old ⌃⇧V binding to the new defaults on first launch of a Session-5+ build.

**What remains (needs Mudit, not code):**
0. **[Session 14] ~~Decide the scroll/click permission trade-off~~ — MOOT, no decision needed.** The misrouting was the fully transparent window being invisible to the window server's per-pixel hit-testing, fixed permission-free with a shape fill (see Session 14). What remains is a 2-minute owner QA: ⌥V → immediate trackpad scroll (app behind must not move), off-center row clicks (must paste, never close spuriously), and an eyeball check that the 6% backing fill is invisible against the glass.
1. **Interactive QA** — press ⌥V and exercise the panel by hand (automated tests can't drive the UI): search, arrows, ⌥Return rich paste, context menu, settings tabs, onboarding flow (delete the `com.mudit.safeclip` defaults domain to re-trigger it). For ⌥C OCR, the first use shows the system **Screen Recording** consent → allow once, then quit & reopen. **New (Session 6):** confirm the panel now opens *above* the cursor with a callout arrow pointing at it (default, no permission); then in Settings → General toggle **"Open above the text cursor"** → the in-app explanation sheet appears, Continue triggers the macOS **Accessibility** prompt (allow once), and the panel should then anchor above the blinking caret in a text field with the arrow at the caret. Denying/leaving it off falls back to mouse anchoring. **Session-6 QA rounds (16 Jun, ROADMAP R16 addenda):** (a) caret-exactness is **source-app dependent** — native/WebKit fields (Perplexity, Instagram, Safari inputs) anchor exactly; Chromium/Electron (Claude Desktop, Arc, Wispr Flow) expose no a11y tree by default. Owner greenlit using the `AXManualAccessibility` lever: `CaretLocator.prewarmFrontmostApp()` switches on the focused Chromium/Electron app's a11y tree (the one AX *write* SafeClip makes — enables reading only, disclosed in the consent sheet); `AppDelegate` pre-warms it on app-activation so the first ⌥V works. Safari/WebKit *web content* still leans on the 3-probe fallback. (b) Panel chrome rebuilt as one SwiftUI `CalloutShape` (`PanelChrome.swift`, `glassEffect` on 26+) — owner confirmed it looks good. (c) Concealed masking: many copies show as `••••` because **Wispr Flow (dictation) pastes into the focused app, recorded as Claude/WhatsApp**, which (plus those apps themselves) tag copies `org.nspasteboard.ConcealedType` (live DB: all concealed rows from those two). Fixes: masked rows **reveal on hover**; new **`ignoreConcealedFrom`** per-source guardrail — right-click a masked row → "Always Show Copies from [App]" (also un-masks existing rows via `HistoryStore.unconcealSource`), or Settings → Privacy → "Apps not treated as passwords". Real password managers stay masked (attributed to themselves).
2. **macOS clipboard prompt** — on some machines the first background capture shows a one-time "paste from other apps" system prompt → choose *Always Allow*. The deny path is handled (capture pauses + menu-bar warning).
3. ~~**Notarized release**~~ **DONE (15 Jul) — pipeline fully works.** `Scripts/release.sh` now does archive→export→sign→notarize→staple→dmg→sparkle-sign end to end; first real run produced `SafeClip-0.2.1.dmg` with status Accepted. What's left is purely distribution: upload the `.dmg` somewhere reachable (real domain not live yet) and add the printed `<enclosure>` block to `appcast.xml`.
4. Open product calls: app name trademark check, website pricing.
5. **[Found in a website session, 3 Jul, NOT investigated or fixed here] Screenshot-capture bypass on the panel.** Session 12/14 set the panel's `sharingType` to `.none` specifically so it's excluded from screen capture (verified `.readOnly` only in DEBUG, `.none` in release). Mudit was able to take a real screenshot of the live panel anyway while pulling a reference image for the website's hero mockup, which shouldn't be possible if that exclusion is working. Needs its own session to reproduce (how was the screenshot taken — `⌘⇧4`/`⌘⇧5`, a third-party tool, screen recording software, something else — since different capture paths respect `sharingType` differently) and root-cause before trusting that protection again. This file's session was scoped to the marketing website only; this was not touched.
6. **[Flagged in a website session, 9 Jul, code feature — NOT built yet] Show each clip's source app in the panel row.** Every clip already stores its source app's bundle ID (captured at copy time; today it's used only in the right-click menu's "Always Show Copies from [App]" and in `unconcealSource`, via `ClipRowView.appName(for:)`). But `ClipRowView` itself doesn't surface it — the row is a single line: leading badge/swatch/thumb + `displayText`, then `trailingDetail` (size / "N chars" / "N files" · relative time) on the right. Implement displaying the source app in the row — ideally the app **icon** (`NSWorkspace.shared.icon(forFile:)` on the resolved bundle path, or by bundle ID) as a small leading/trailing glyph, with the app **name** as a fallback/tooltip. Decide placement so it doesn't crowd `trailingDetail`. Came up while making the website mockup match the real row; the marketing mock deliberately mirrors today's reality (no source app shown, size/chars on the right) until this ships.
7. **[Session 18, confirmed hit-in-practice Session 19 — NOT built yet] Onboarding doesn't re-show on a fresh reinstall, and Uninstall doesn't clean up permissions.** macOS does not wipe `UserDefaults` or history when the `.app` is deleted (same bundle ID ⇒ same defaults domain), so a redownload with old files still on disk skips onboarding entirely — the owner hit this personally in Session 19 (redownloaded from the DMG, onboarding never appeared). This matters beyond tidiness: onboarding's permissions step is the owner's only convenient path to (re)grant Accessibility/Screen Recording, so skipping it means fighting macOS TCC by hand — the owner's exact reported friction is 5 manual steps (remove from Accessibility in System Settings → reopen the app → re-trigger the request → toggle back on → reopen again). **Two pieces, recommended fix directions (neither implemented):** (a) an **install-fingerprint gate** — key onboarding's re-show off the running bundle's creation date (`Bundle.main.bundleURL`), not `UserDefaults`, so a fresh copy always re-triggers it regardless of stale prefs/history; (b) a proper **Uninstall** menu item that also shells out to `tccutil reset Accessibility com.mudit.safeclip` (+ `ScreenCapture`) so a reinstall doesn't inherit stale TCC grants — unverified whether this needs elevated privileges on the owner's macOS version, check when building. Full detail: memory `project_safeclip_reinstall_onboarding`.

**Distribution (revised Session 5 — paid website binary):** sell the **non-sandboxed `SafeClip` build** (Developer ID, notarized `.dmg`) as a **paid download from Mudit's own website** behind a payment gateway. The binary is **not** offered free on GitHub Releases; the MIT source stays public for the build-from-source / audit path. **Mac App Store is deferred** — earn website revenue → fund the Apple Developer license → then ship the `SafeClip-MAS` (sandboxed) target. Until then MAS is dormant; do **not** let sandbox limits block features (screen-OCR shells out to `/usr/sbin/screencapture`, which the sandbox forbids — fine for now). One repo, two targets, shared sources — do NOT fork into separate folders. Channel detail: [docs/DISTRIBUTION.md](docs/DISTRIBUTION.md).

### What this project is
A **privacy-first macOS clipboard manager**. The differentiators — none of which any mainstream competitor (Maccy, Paste, CleanClip, CopyClip, Raycast) currently ships:
1. **Encrypted history store** — AES-256-GCM, key in macOS Keychain.
2. **Floating panel at the cursor** — like the emoji picker (⌃⌘Space), triggered by a global shortcut. Not a menu-bar dropdown.
3. **Plain-text paste by default** — rich formatting captured but stripped on paste; `⌥Return` keeps formatting.
4. **Screen-recording privacy** — history blurred while screen sharing (unbuilt elsewhere; Maccy #1017 open since Jan 2025).
5. **ClickFix / pastejacking detection** — warn when the clipboard was overwritten by a webpage with a shell command (novel; ClickFix was >50% of macOS malware-loader activity in 2025).
6. **Screen-region OCR (⌥C)** — drag a region like ⌘⇧4; the text in it is recognized on-device (Vision) and copied to the clipboard. The screenshot is OCR'd from a temp file then deleted — never stored. Interactive `screencapture -i` keeps the app itself free of Screen Recording permission.

### What was decided this session (do NOT re-litigate without reason)
- **No synthesized ⌘V → no Accessibility permission.** App writes to the pasteboard; the *user* presses ⌘V. One extra keypress, but the app launches with **zero special permissions**. This is a core design pillar.
- **"Delete after use" is a per-item action ("burn after paste"), NOT the global default.** History persists by default — that's the whole point of a clipboard manager. Burn is opt-in per item.
- **Plain-text is the default paste, but rich text is also stored** so `⌥Return` can paste formatting. Forcing plain-text-only breaks tables/docs.
- **Source-app filtering (skip 1Password etc.) and pattern detection are opt-in, OFF by default** — the user copies passwords often and wants them captured.
- **The "paste window" is a real, unfixable limitation** — plaintext sits on `NSPasteboard` for <1s during paste. We **disclose** it honestly (TERMS §3); we never claim "delete after use" is cryptographic.
- **Local-only, open source (MIT).** No backend, no telemetry. HN users won't trust a closed-source clipboard manager.

### ⚡ MILESTONE STATE (all code shipped 10 June 2026)
- ~~M0 scaffold~~ ✅ xcodegen project, SPM deps wired, menu-bar shell, zero-warning build
- ~~M1 capture + encrypted store~~ ✅ AES-256-GCM + Keychain + GRDB, live-verified with `strings`
- ~~M2 floating panel~~ ✅ non-activating cursor panel, search, plain/⌥rich paste, full keyboard nav
- ~~M3 polish~~ ✅ onboarding w/ terms consent, settings (4 tabs), login item, expiry, clear-all — **notarization pending Developer ID cert** (`Scripts/release.sh` ready)
- ~~M4 privacy~~ ✅ burn-after-paste, screen-record heuristic + manual Privacy Mode, exclusion list
- ~~M5 detection~~ ✅ ClickFix warnings, opt-in pattern detection, concealed-password masking, pinning
- ~~v0.2.0 (Session 3)~~ ✅ **images + file copies in history** (encrypted payloads + thumbnails, schema v2 migration) and **Liquid Glass** panel chrome on macOS 26+ (material fallback ≤25). See ROADMAP R13.
- ~~Session 5~~ ✅ **screen-region OCR (⌥C)** via `ScreenOCRService` (interactive `screencapture` → Vision → clipboard, image deleted, never stored) + non-activating menu-bar confirmation toast; **shortcut remap** ⌃⇧V→⌥V with one-time migration; **classifier fix** — tightened the low-confidence "possible API key" heuristic (≥40 chars, mixed upper/lower/digit, Shannon entropy ≥3.2) so hex hashes/UUIDs/SHAs stop false-flagging, plus a **Reset All Flags** action (Settings → Advanced) to un-mask already-mislabeled rows. The earlier auto-OCR-on-image-copy was removed (silent/no UI). 43 tests.
- ~~Session 6~~ ✅ **caret-anchored panel + callout arrow** (ROADMAP R16). Default (no permission): the panel now opens *above* the cursor with an arrow pointing at it, so it never lands on the field you're pasting into. New **opt-in** "Open above the text cursor" toggle (Settings → General) anchors it above the blinking caret instead, via the **read-only** Accessibility caret bounds (`CaretLocator`: `kAXFocusedUIElement`→`kAXSelectedTextRange`→`kAXBoundsForRange`) — an in-app explanation sheet shows *before* the macOS prompt (R1 transparency-first), and it gracefully falls back to mouse anchoring when off/not-granted/no-caret. **Still never synthesizes ⌘V.** Sandbox-gated like OCR (`CaretLocator.isSupported` → GitHub channel only; MAS disables the toggle). Zero warnings, 43 tests.
- ~~Session 7~~ ✅ **warm-up feature bundle** (first slice of the safety-led backlog). (1) **ClickFix detection ON by default** (`AppSettings.clickFixDetection` now `true` + one-time `clickFixDefaultOnMigrated` flag enables it for existing installs). (2) **Numeric quick-paste** `⌃⌘1…⌃⌘9`, `⌃⌘0` — places the top-10 history items on the clipboard for the user's own ⌘V (no synthesized paste; pillar-safe), with a menu-bar confirmation toast and per-slot rebind/clear in Settings → General. Pastejacking-flagged items aren't placed silently (toast directs to the panel). (3) **Color swatches + SVG badge** — `ClipColor` parser (hex/rgb/rgba, whole-string) + `isLikelySVGMarkup` in `SafeClipCore` (9 new tests); the panel row renders a 24pt swatch for color clips and a code glyph for SVG markup. *Note: this is the display-only slice of "SVG kind" — no capture-pipeline/rendered-preview SVG yet.* (4) **Blur-not-blank** — while screen-recording/Privacy Mode the panel now shows the list **blurred** (radius 10) + non-interactive with an "Hidden while screen recording" pill, instead of the old blank placeholder. Both targets build zero-warning, 52 tests. **Interactive QA pending** (see below).
- ~~Session 7 (cont.)~~ ✅ **medium tier of the backlog (#5–#8).** (5) **Categories / collections** — encrypted per-clip `category` (schema v3 + `HistoryStore.setCategory`), a filter-chip bar in the panel, and a context-menu submenu (assign / New Category… via NSAlert / remove). Filtering is in-memory in `PanelViewModel.recomputeFilter`. (6) **Multipaste** — ⌘-click builds an ordered selection (numbered badges + count bar); Return places the clips combined (newline-joined) as one payload for a single ⌘V (`AppState.pasteCombined` → `PasteService.placePlainText`; pillar-safe, warns once if any item is ClickFix-flagged). (7) **Menu-bar history** — the status-item menu now lists the top 8 recent clips (icons, masked concealed values, suppressed while history hidden); clicking places for ⌘V, ClickFix items route to the panel. (8) **Search-within-images** — copied images are OCR'd on-device (`OCRService`) into an encrypted `ocr_text` column (schema v4) that the panel search matches; new "Search text inside images (OCR)" toggle (default on) in Settings → General. Both targets zero-warning, **55 tests** (added category + OCR round-trip/encryption tests). **Remaining backlog (private notes): #9 saved snippets (Tier-A), #10 structure-aware OCR, #11 auto-expand snippets (AX-gated), #12 E2E sync (architectural) — each warrants a focused session. Interactive QA pending.**

- ~~Session 9~~ ✅ **panel-scroll fix + saved snippets (#9).** **Panel scroll** — `FloatingPanel` dropped `.nonactivatingPanel`; the panel activates SafeClip on show (so the SwiftUI list gets `scrollWheel` via the responder chain) and `FloatingPanelController` restores focus to the previously-frontmost app on hide (Escape/paste/toggle restore; click-outside doesn't), keeping ⌘V landing in the user's document. Removed the dead live-hit-test scroll monitor; still never synthesizes ⌘V. **Saved snippets (#9, Tier-A)** — encrypted `snippets` table (schema **v5**), `HistoryStore` CRUD/reorder/`fetchSnippets`, `Snippet` model, `AppState.pasteSnippet` (places body via `PasteService.placePlainText` for ⌘V, not re-captured), **Settings → Snippets** tab (`SnippetsSettingsView`), and a **"Snippets" chip** in the panel chip-bar switching the list to snippet mode (search + arrows + Return; ⌘⌫/⌘P inert; blurred while screen-recording). **Magnifier eyedropper** — owner's icon → `Assets.xcassets/ColorPickerLoupe`; `ColorLoupe.swift` (`LoupeSampler` actor + `ColorLoupeView`) + rewritten `ScreenColorPicker` (cursor-following loupe, live SCK capture excluding own windows, click→hex, Esc cancel), wired into `AppDelegate.pickColor` with `NSColorSampler` fallback until Screen Recording granted. **Scroll DEBUG tracker** — passive `scrollWheel` logger to diagnose the activating-panel scroll. Both targets zero-warning, **61 tests**. **Interactive QA pending** (scroll logs + focus restoration; eyedropper loupe on a real display; snippets). **Onboarding revamp** awaiting owner's mockup (TERMS-only, no separate privacy policy).

- ~~Session 10~~ ✅ **batch (all awaiting owner QA).** (1) **Scroll fix #2** — `FloatingPanelController.forwardScroll` forwards each `scrollWheel` to a cached `NSScrollView` and consumes it (events arrive but SwiftUI won't scroll; hit-test unreliable mid-gesture). (2) **Eyedropper v2** — rebuilt for safety after a freeze: watchdog/resign-active/global+local Esc+click teardown, visible cursor; v2 icon, tip pick-point, bottom-edge flip, hex up, color ring, Liquid Glass, 140pt (`ColorLoupe.swift`, `ScreenColorPicker.swift`). (3) **Onboarding wizard** — 6-step dark-glass card to the owner's mockup (`OnboardingView.swift`); **TERMS.md bundled** (`project.yml`) + opened locally. (4) **#11 auto-expand snippets** — encrypted `trigger` (schema **v6**), `SnippetExpander` (global keyDown → synth backspaces + unicode insert; own events tagged), opt-in `expandSnippets` + AX-gated + sandbox-excluded; trigger field + toggle in Settings → Snippets. Both targets zero-warning, **62 tests**.

Full milestone detail + deltas from plan: [docs/ROADMAP.md](docs/ROADMAP.md).

---

## Files in this folder
| Path | Purpose |
|------|---------|
| [PRD.md](PRD.md) | Full product spec — features (P0/P1/P2 w/ acceptance criteria), UX, security architecture, data model, milestones, risks, testing, research appendix with sources. |
| [TERMS.md](TERMS.md) | Terms of Use / liability disclaimer. Linked from onboarding + README. Discloses the paste-window limit. |
| [README.md](README.md) | Public pitch: gap table, security model (honest version), install + build-from-source. |
| [SECURITY.md](SECURITY.md) | Private-advisory process; in/out of scope. |
| [LICENSE](LICENSE) | MIT. |
| CLAUDE.md | This file. |
| [docs/DESIGN.md](docs/DESIGN.md) | App architecture — the three UI surfaces as one app, SwiftUI/AppKit split, data flow. |
| [docs/ROADMAP.md](docs/ROADMAP.md) | Milestones (now with status), research log, decisions, risk register. |
| `SafeClipCore/` | SPM package: crypto, Keychain, GRDB store, security scanner, snippets, OCR layout, sync engine. **74 tests** — `swift test --package-path SafeClipCore`. |
| `App/` | App sources: `main.swift` (AppKit entry), AppDelegate, AppState, Services/ (incl. `OCRService`, `ScreenOCRService`, `UpdateService` — Sparkle, GitHub channel only, `Feedback` — mailto: feedback), MenuBar/, Panel/, Settings/ (incl. `UpdatesSettingsView`), Onboarding/ (incl. `OnboardingVideoPlayer`), OnboardingVideos/ (drop-in `.mp4`s, see its README). |
| `project.yml` | XcodeGen spec — regenerate `SafeClip.xcodeproj` with `xcodegen` after editing. |
| `Config/` | Info.plist (`LSUIElement`, Sparkle `SU*` keys), entitlements (empty by design — no sandbox, zero permissions). |
| `Scripts/` | `smoke_test.sh` (live security assertions), `release.sh` (sign→notarize→dmg→**sparkle-sign**), `sparkle_sign.swift` (Ed25519-signs a release `.dmg` from the Keychain-held key). |
| `.github/workflows/ci.yml` | CI: core tests + zero-warning build gate. |

---

## Tech stack (locked — see PRD §11)
| Layer | Choice |
|-------|--------|
| Language | Swift 6 (strict concurrency) |
| UI | SwiftUI (panel + settings) + AppKit (`NSPanel`, `NSStatusItem`) |
| Min / target OS | macOS 14 (Sonoma) / macOS 16 pasteboard-privacy API |
| Encryption | CryptoKit `AES.GCM` 256-bit, per-item nonce |
| Key storage | Security framework Keychain, `kSecAttrAccessControl` locked to code signature |
| DB | SQLite via `GRDB.swift` (MIT) |
| Hotkey | `KeyboardShortcuts` by Sindre Sorhus (MIT), public APIs only |
| Cursor pos | `NSEvent.mouseLocation` |
| Panel | `NSPanel`, `.nonactivatingPanel`, `isFloatingPanel = true`, clamp to `visibleFrame` |
| Login item | `SMAppService` (Debug default OFF, Release ON — see caveats) |
| OCR | Vision `VNRecognizeTextRequest` (on-device) + interactive `screencapture -i` for region select |
| Dist | Paid notarized `.dmg` on Mudit's website (payment gateway); MIT source public for build/audit; MAS deferred |
| Updates | `Sparkle` 2.x (MIT), GitHub channel only — appcast at `safeclip.app/appcast.xml`, Ed25519 signing key in the login Keychain |

---

## Build / run
```bash
xcodegen                                                      # only after editing project.yml
xcodebuild -project SafeClip.xcodeproj -scheme SafeClip -configuration Debug build      # GitHub variant
xcodebuild -project SafeClip.xcodeproj -scheme SafeClip-MAS -configuration Debug build  # App Store variant (sandboxed)
open SafeClip.xcodeproj                                       # or work in Xcode
swift test --package-path SafeClipCore                        # core test suite (74 tests)
Scripts/smoke_test.sh                                         # live security assertions (wipes local history!)
```
**Run the RIGHT build (Session-5 lesson):** only ever launch via Xcode ⌘R or `open` the DerivedData `Debug/SafeClip.app`. Do **not** `xcodebuild -derivedDataPath build …` into the repo — that drops stale `.app` copies in `build/` that Spotlight/launchd then launch instead of your latest build (the sandboxed MAS copy was hijacking ⌥C). Debug builds now default `launchAtLogin` **OFF** so a dev build can't register itself as a login item and resurrect itself. To wipe a tangle: kill `SafeClip`, `rm -rf build ~/Library/Developer/Xcode/DerivedData/SafeClip-*`, `tccutil reset All com.mudit.safeclip`, then clean-build.
**Working rule (how Mudit likes to work):** after any change, build and fix all warnings/errors before calling a task done. Zero warnings — CI enforces it.

### Security smoke tests (verified passing 10 June 2026)
```bash
strings ~/Library/Application\ Support/SafeClip/history.db   # must show NO clipboard text ✅
security find-generic-password -s SafeClip                    # key present ✅
```

---

## Critical caveats to keep in mind
- **Migrations vs. code is not a thing here** (no DB server) — but **notarization is**: a code change isn't shippable until the binary is re-signed + notarized. Source push ≠ released build.
- **macOS 16 will change clipboard capture** — abstract capture behind a protocol with two impls (`changeCount` polling for ≤15, `detect`-before-read for 16+). Test on the 15.4 preview now. (PRD §15.)
- **Never claim zero-knowledge against the OS.** The encryption protects the on-disk store, not the live system clipboard (which Universal Clipboard syncs across devices).
- **Keep scope minimal.** No AI, no sync v1, no browser extension. (PRD §13.) Images/files were added in v0.2.0 by owner decision — encrypted like text, paths-not-contents for files. Screen-region OCR added Session 5 — on-device Vision only, the screenshot is never persisted.
- **Screen-OCR is non-sandboxed-only.** `ScreenOCRService` spawns `/usr/sbin/screencapture`, which the App Sandbox forbids; it self-reports `.unavailable` (env `APP_SANDBOX_CONTAINER_ID`) and shows a toast in a sandboxed build. Fine while MAS is deferred.

---

## Open questions (PRD §19)
- **App name** — "SafeClip" is a working title; check trademark + macOS app-name collisions.
- ~~**Paid vs. free**~~ **Decided (Session 5): paid notarized `.dmg` sold on Mudit's own website via a payment gateway; MIT source stays public for build/audit.** Open: exact price, which gateway.
- ~~**Sandbox stance** — decide at M0.~~ **Decided: non-sandboxed .dmg is the shipping build; MAS deferred until website revenue funds the Apple Developer license.**
- **macOS 16 launch timing** — aim release near macOS 16 GA (fall 2026) for the privacy-narrative tailwind.

---

## Context: how this project started
Mudit uses CopyClip and wanted a clipboard manager that (a) pastes plain text via a special trigger, (b) shows history in a cursor-anchored popup like the emoji viewer, and (c) protects sensitive data (passwords) rather than hoarding everything in plaintext. Research confirmed the exact combination doesn't exist and the encryption + screen-share + hijack-detection gaps are uncontested. This is a separate project from the De-Sludging Project (different folder, different repo) — they share nothing.
